export const environment = {
  production: true, // staging sera en ligne, donc app buildée aussi !
  name: 'staging',
  apiUrl: 'staging.pokecarte-api.loutredev.fr' // Ex : staging-api.fromphacotohero.fr
};
